'use strict';

angular.module('app')
  .controller('CompaniesCtrl', function ($rootScope, $scope) {
    $rootScope.pageTitle = 'Companies'; // hmmm.....
    // $scope.companies  = [10, 20, 30, 40];
});
